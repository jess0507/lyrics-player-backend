# Cloud Functions 環境變數(firebase 部署時自動載入;本檔已被 .gitignore 忽略)。

# aeneas-align 的 Cloud Run 根 URL:無尾斜線、不含 /align。
# ⚠️ 請替換為實際部署的服務 URL(gcloud run services describe aeneas-align
#    --region asia-east1 --format='value(status.url)')。
ALIGN_SERVICE_URL=https://aeneas-align-833102634982.asia-east1.run.app

# 每使用者每日對時次數上限(選填,預設 20)。
ALIGN_RATE_LIMIT_PER_DAY=20

# whisperx-align 的 Cloud Run 根 URL(generate_lyrics 用):無尾斜線、不含 /transcribe。
WHISPERX_SERVICE_URL=https://whisperx-align-ltro7kk2pq-de.a.run.app

# 測試帳號 merukoo1994@gmail.com 不受每日配額限制(QA 用)
RATE_LIMIT_EXEMPT_UIDS=HZBtmUsSpkeAHr7B6xNoTIPwvKH3

TASKS_INVOKER_SERVICE_ACCOUNT=833102634982-compute@developer.gserviceaccount.com
